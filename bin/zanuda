#!/usr/bin/env bash
# Wrapper that runs Zanuda CLI via tsx from any working directory.
# The tsx binary is installed locally (npm ci), no global install needed.
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec npx --no-install tsx "$SCRIPT_DIR/../src/cli.ts" "$@"
